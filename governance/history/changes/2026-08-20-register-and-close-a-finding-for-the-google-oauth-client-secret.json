{
  "adr_refs": [],
  "affected_components": [],
  "affected_contracts": [],
  "behavioral_effects": [
    "The Google OAuth client secret exposure is on the permanent record with disposition resolved; open findings count is unchanged since it is registered and closed in the same record."
  ],
  "compatibility_impact": "None. No code or config changes; the OAuth client keeps its client ID, and no environment currently consumes GOOGLE_DRIVE_CLIENT_SECRET.",
  "date": "2026-08-20",
  "files_changed": [],
  "follow_up": [
    "The history rewrite session removes all three dead credential strings from backend/.env.example history and regenerates the gitleaks baseline."
  ],
  "id": "2026-08-20-register-and-close-a-finding-for-the-google-oauth-client-secret",
  "initiated_by": {
    "kind": "agent"
  },
  "limitations": [
    "Reset verified by owner statement, not tooling. The dead GOCSPX string remains in remote history until the rewrite session that follows this one."
  ],
  "manifests_changed": [],
  "reason": "The 2026-08-19 unbaselined full-history gitleaks scan showed the known leak commit also exposed GOOGLE_DRIVE_CLIENT_SECRET, never itemized by the secrets-pushed-to-remote finding; the owner reset the OAuth client secret and cleaned its redirect URIs on 2026-08-20, so the exposure is registered and resolved in one record for an honest history",
  "risks": [],
  "rollback": "git revert the commit; re-opening requires a new record with status open and a later date.",
  "schema_version": 1,
  "security_impact": "Positive: the third and last credential exposed by the historical leak commit is now dead. The owner reset the Google OAuth client secret and removed the developers.google.com/oauthplayground redirect URI, closing the leaked-secret-plus-open-playground token-minting combination. No live credential remains exposed in git history.",
  "summary": "Register and close a finding for the Google OAuth client secret leaked in backend/.env.example history",
  "tool_version": "1.0.0",
  "validators_run": [
    {
      "evidence_ref": "schema validation over the edited record; 0 blocking",
      "id": "governance-check",
      "result": "pass"
    },
    {
      "evidence_ref": "unbaselined gitleaks v8.30.1 over all 135 reachable commits found exactly 3 findings, all in commit aaec6463 backend/.env.example; no other credential appears anywhere in history",
      "id": "secret-scan",
      "result": "pass"
    }
  ],
  "findings": [
    {
      "id": "google-oauth-client-secret-in-history",
      "severity": "high",
      "status": "resolved",
      "disposition": "rotate-at-provider",
      "detail": "Commit aaec6463 (2026-08-02) exposed GOOGLE_DRIVE_CLIENT_SECRET (GOCSPX-...) at backend/.env.example line 155, alongside the two credentials covered by secrets-pushed-to-remote; the 2026-08-19 unbaselined full-history gitleaks scan surfaced it as the third baseline fingerprint, never separately itemized. The owner reset the OAuth client secret in Google Cloud Console on 2026-08-20 and removed the oauthplayground redirect URI from the client. The dead string remains in remote history until the history rewrite; no environment consumes the variable today."
    }
  ]
}
