{
  "schema_version": 1,
  "policy_group": "architecture",
  "description": "Component boundaries and dependency direction. Every rule here starts advisory: none has been run against real changes yet, and a rule that fires wrongly in its first week gets the whole system ignored. Promotion criteria say what would earn strictness.",
  "rules": [
    {
      "id": "component-declared-once",
      "statement": "Each component is declared in exactly one place: a directory annotation or the curated manifest, never both.",
      "rationale": "Two declarations are two sources of truth for the same intent, which is the failure this system exists to prevent.",
      "maturity": "advisory",
      "check_impl": "checks.architecture.check_single_declaration",
      "promotion_criteria": "No false positive across ten governed changes. This one is cheap to promote because the condition is unambiguous.",
      "exceptions_allowed": false,
      "exception_refs": []
    },
    {
      "id": "dependencies-resolve",
      "statement": "Every allowed or forbidden dependency names a declared component, and no component is both allowed and forbidden for the same dependent.",
      "rationale": "A dependency rule naming a component that does not exist reads as protection while enforcing nothing.",
      "maturity": "advisory",
      "check_impl": "checks.architecture.check_dependency_declarations",
      "promotion_criteria": "No false positive across ten governed changes.",
      "exceptions_allowed": false,
      "exception_refs": []
    },
    {
      "id": "layering-routes-services-repositories",
      "statement": "Routes call services; they never import or call repositories directly.",
      "rationale": "The service layer owns transaction boundaries and domain exceptions. A route reaching past it gets neither.",
      "maturity": "advisory",
      "check_impl": "checks.graph.check_layering",
      "promotion_criteria": "The import graph exists and the rule runs clean on the current tree.",
      "exceptions_allowed": true,
      "exception_refs": [],
      "authoritative_source": ".claude/rules/architecture.md"
    },
    {
      "id": "forbidden-dependency-not-taken",
      "statement": "No component imports or calls a component listed in its forbidden_dependencies.",
      "rationale": "Declaring a boundary and not enforcing it is worse than not declaring one, because it creates false confidence.",
      "maturity": "advisory",
      "check_impl": "checks.graph.check_forbidden_dependencies",
      "promotion_criteria": "The import graph exists; the rule runs clean; the declared boundaries have been reviewed against reality at least once.",
      "exceptions_allowed": true,
      "exception_refs": []
    },
    {
      "id": "rest-goes-through-the-proxy",
      "statement": "Every REST call from the browser goes through a Next.js proxy route handler; the chat WebSocket is the only direct browser-to-backend edge.",
      "rationale": "The proxy hop is part of every REST impact chain. Impact analysis that skips it reports the wrong blast radius, and a change that adds a second direct edge changes the security surface without looking like it does.",
      "maturity": "advisory",
      "check_impl": "checks.architecture.check_proxy_coverage",
      "promotion_criteria": "The proxy mapping extractor lands and reproduces the reviewed baseline.",
      "exceptions_allowed": true,
      "exception_refs": [
        "ws-chat-bypasses-proxy"
      ]
    },
    {
      "id": "services-declare-healthchecks",
      "statement": "Long-running services declare a healthcheck, or reference an exception explaining why they do not.",
      "rationale": "Compose dependency ordering is only meaningful when readiness is observable. A missing healthcheck turns depends_on into a guess.",
      "maturity": "advisory",
      "check_impl": "checks.architecture.check_healthchecks",
      "promotion_criteria": "The Compose extractor lands and every current omission is either fixed or has an exception.",
      "exceptions_allowed": true,
      "exception_refs": [
        "taskiq-healthchecks-disabled"
      ]
    },
    {
      "id": "no-orphaned-surfaces",
      "statement": "Routes, configuration variables, tools, and database tables have a consumer, or a classification explaining why they do not.",
      "rationale": "Unused surface is either dead weight or an unfinished feature, and the two need opposite responses. Generator-provided but feature-disabled surfaces are neither, which is why the classification exists.",
      "maturity": "advisory",
      "check_impl": "checks.graph.check_orphans",
      "promotion_criteria": "Orphan detection runs with the generator-feature whitelist and produces no false positive on the current tree. The check covers the module slice today; route and tool consumer analysis arrives with the phase-7 site join.",
      "exceptions_allowed": true,
      "exception_refs": [
        "billing-generator-feature-disabled"
      ]
    },
    {
      "id": "no-broken-site-chains",
      "statement": "No page calls an API path that neither a proxy handler nor a backend route serves.",
      "rationale": "A call into the void fails only at runtime, on the user's screen. The site graph can see it statically, so waiting for the 404 is a choice.",
      "maturity": "advisory",
      "check_impl": "checks.graph.check_broken_site_chains",
      "promotion_criteria": "The four known broken-chain pages are fixed or their calls classified, and the rule then runs clean for a release cycle.",
      "exceptions_allowed": true,
      "exception_refs": []
    },
    {
      "id": "no-dependency-cycles",
      "statement": "No strongly connected component larger than one module exists in either language's runtime import graph.",
      "rationale": "A cycle means neither side can be understood, tested, or replaced alone. One closed at module scope works only while initialization order happens to hold; one closed through deferred imports is the same coupling wearing a workaround.",
      "maturity": "advisory",
      "check_impl": "checks.graph.check_dependency_cycles",
      "promotion_criteria": "The known cycles on the current tree are either broken or recorded as deliberate in an ADR, and the rule then runs clean for a full release cycle.",
      "exceptions_allowed": true,
      "exception_refs": []
    },
    {
      "id": "thick-domains-expose-a-facade",
      "statement": "A thick service subpackage is imported only through its package root; sub-modules stay package-internal.",
      "rationale": "The point of the subpackage boundary is that callers depend on the domain, not on how it is currently arranged inside.",
      "maturity": "advisory",
      "check_impl": "checks.graph.check_thick_domain_facades",
      "promotion_criteria": "The import graph exists and the rule runs clean.",
      "exceptions_allowed": true,
      "exception_refs": [],
      "authoritative_source": ".claude/rules/architecture.md"
    }
  ]
}
